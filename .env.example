# Google Gemini — required for smart week generation (Vercel serverless only).
GEMINI_API_KEY=your_api_key_here

# Optional. Default: gemini-2.5-flash (change if you hit quota errors on 2.0-flash)
# GEMINI_MODEL=gemini-2.5-flash
